<h1>Your Shopping Cart</h1>

<ul>
  <% cart.each do |item| %>
  <li><%= item %></li>
  <% end %>
</ul>

<h3>Add Item(s) to Cart</h3>
<%= form_tag('/add') do %>
	<%= label_tag :product %>:
	<%= text_field_tag :product %>
	<%= submit_tag "add to cart" %>
<% end %> 
